- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/bat:
      create: true
      path: config/bat
    ~/.gitconfig:
    ~/.mackup.cfg:
    ~/.nanorc:
    ~/.zprofile:
    ~/.zlogin:
    ~/.zlogout:
    ~/.zshenv:
    ~/.zshrc:

- create:
    - ~/Work

- shell:
    - command: ./setup/homebrew.zsh
      stdout: true
      stderr: true
    - command: ./setup/zsh.zsh
      stdout: true
      stderr: true
    - command: ./setup/node.zsh
      stdout: true
      stderr: true
    - command: ./setup/python.zsh
      stdout: true
      stderr: true
