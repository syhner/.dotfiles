# AppleScript: do shell script "~/.dotfiles/open/work.sh"

open ~/work/main.code-workspace
open -a "Google Chrome" --args --profile-directory="work" # Profile name from "~/Library/Application Support/Google/Chrome/work"
open -a "Slack"
